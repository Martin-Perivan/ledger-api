/**
 * Ledger entry direction: money leaving (DEBIT) or entering (CREDIT) an account.
 * @module domain/enums/entry-type
 */

enum EntryType {
  DEBIT = "DEBIT",
  CREDIT = "CREDIT",
}

export { EntryType };
